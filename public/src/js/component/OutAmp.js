


    var mainout = new DSX.Amp({
        gain: 0.9
    }); // amp out
    var gain = new DSX.Amp({
        gain: 0.9
    }); // amp out
    var gain2 = new DSX.Amp({
        gain: 0.9
    }); // amp out
    var gain3 = new DSX.Amp({
        gain: 0.9
    }); // amp out
    var gain4 = new DSX.Amp({
        gain: 0.9
    }); // amp out
    var gain5 = new DSX.Amp({
        gain: 0.9
    }); // amp out
    var gain6 = new DSX.Amp({
        gain: 0.9
    }); // amp out
    var gain7 = new DSX.Amp({
        gain: 0.9
    }); // amp out
    var gain8 = new DSX.Amp({
        gain: 0.9
    }); // amp out
    var gain9 = new DSX.Amp({
        gain: 0.9
    }); // amp out
    var gain10 = new DSX.Amp({
        gain: 0.9
    }); // amp out
    var mixgain = new DSX.Amp({
        gain: 0.9
    }); // amp out
    var saxsub = new DSX.Amp({
        gain: 0.0
    }); // amp out
    var bypassgain = new DSX.Amp({
        gain: 0.9
    }); // amp out
    var saxgain = new DSX.Aux({
        gain: 0.9
    }); // amp out
    var oscGain = new DSX.Aux({
        gain: 0.9
    }); // amp out
    var nullGain = new DSX.Aux({
        gain: 0.9
    }); // amp out
    var amGain = new DSX.Aux({
        gain: 0.9
    }); // amp out
